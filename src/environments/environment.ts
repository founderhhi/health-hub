export const environment = {
  production: false,
  apiBaseUrl: '/api',
  dailyDomain: 'https://healthhub.daily.co',
  // Stripe publishable key is fetched at runtime from /api/payments/publishable-key
  stripePublishableKey: ''
};
